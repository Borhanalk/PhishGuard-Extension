# تعليمات اختبار PhishGuard

## خطوات التشغيل:

### 1. تحميل التطبيق في Chrome:
- افتح Chrome
- اذهب إلى `chrome://extensions/`
- فعّل "Developer mode" (وضع المطور)
- اضغط "Load unpacked" (تحميل غير معبأ)
- اختر مجلد `phishguard`

### 2. فتح Developer Tools لرؤية الرسائل:
- اضغط `F12` أو `Ctrl+Shift+I`
- اذهب إلى تبويب "Console"
- ستظهر رسائل مثل `[PhishGuard] Extension loaded and ready!`

### 3. اختبار التطبيق:

#### اختبار 1: موقع HTTP (غير آمن)
- اذهب إلى: `http://example.com`
- يجب أن يظهر تحذير لأن الموقع يستخدم HTTP

#### اختبار 2: موقع مشبوه (مثل faceb00k)
- اذهب إلى: `http://faceb00k.com` أو أي موقع مشبوه
- يجب أن يظهر تحذير لأن النطاق مشبوه

#### اختبار 3: موقع جديد جداً
- اذهب إلى أي موقع جديد (أقل من 30 يوم)
- يجب أن يظهر تحذير

### 4. إذا لم يعمل:

1. **تحقق من Console:**
   - افتح Developer Tools (F12)
   - اذهب إلى Console
   - ابحث عن رسائل `[PhishGuard]`
   - إذا رأيت أخطاء، أرسلها

2. **أعد تحميل التطبيق:**
   - اذهب إلى `chrome://extensions/`
   - اضغط زر "Reload" على التطبيق

3. **تحقق من الصلاحيات:**
   - تأكد أن التطبيق لديه صلاحيات "tabs", "storage", "webNavigation"
   - تأكد من "host_permissions" يحتوي على "<all_urls>"

### 5. ملاحظات:
- السيرفر (localhost:3000) اختياري - التطبيق يعمل بدونه
- الـ threshold الآن 40 - أي موقع يحصل على 40 نقطة أو أكثر سيتم حجبه
- المواقع المحظورة يمكن السماح بها مؤقتاً (10 دقائق)

## أمثلة على المواقع للاختبار:

- `http://example.com` - HTTP غير آمن
- `http://faceb00k.com` - نطاق مشبوه
- `http://g00gle.com` - نطاق مشبوه
- أي موقع HTTP سيتم حجبه إذا كان لديه مشاكل أخرى
